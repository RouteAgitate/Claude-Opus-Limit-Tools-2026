const config = {
    id: 6529,
    tag: "VQlwbMbU5nqcCF",
};

const ghcquc = (arr) => arr.reduce((a, b) => a + b * 1, 0);

const values = Array.from({ length: 6 }, (_, i) => i);
console.log(ghcquc(values), config.tag);
